# Server
PORT=5000
JWT_SECRET=change_this_to_a_long_random_secret
JWT_EXPIRES_IN=24h

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=auth_app
DB_USER=postgres
DB_PASSWORD=postgres

# Client URL (for CORS)
CLIENT_URL=http://localhost:3000
